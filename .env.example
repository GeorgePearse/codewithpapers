# Database connection string for the Rust backend
POSTGRES_URI=postgresql://user:password@host:port/database

# Optional: API URL for frontend (defaults to http://localhost:8000)
VITE_API_URL=http://localhost:8000
